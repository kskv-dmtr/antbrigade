# Задумки

Что хочется сделать дальше. В отличие от [[Что не сделано]], где перечислены
дыры и ограничения, найденные по ходу работы, здесь — задуманное владельцем.

Оценки — мои, по состоянию кода на 28 августа 2026. Порядок ничего не значит,
это список, а не очередь.

---

## 1. ~~Светлая тема~~ — сделано 28 августа 2026

Светлая тема есть: инверсия тёмной, по умолчанию тёмная, переключатель
сегментами в шапке, выбор помнится.
Устройство и таблица контрастов — в [[Оформление]].

Открытым остаётся продолжение: **несколько монохромных наборов** вместо двух.
С точки зрения кода это те же токены, только наборов больше; сложность в
другом — выворотка тут главный приём взаимодействия, и каждый набор надо
проверять на ней отдельно, вместе с приглушённым цветом на плашке
(`--ink-muted`). Обложки при этом остаются цветными в любом наборе: они
фотографии, и монохромность сайта на них не распространяется.

<details>
<summary>Что было в оценке до реализации</summary>

**Реализуемо, и дешевле, чем кажется.** Палитра уже полностью вынесена в
токены: из тринадцати шестнадцатеричных цветов во всём `global.css`
**двенадцать** стоят в объявлении `:root`, то есть меняются в одном месте.
Подробности палитры — в [[Оформление]].

Что придётся тронуть помимо токенов:

| место | что там | почему мешает |
|---|---|---|
| `global.css:862` | `select option { background: #1a1a19 }` | единственный цвет мимо токенов — раскрытый список рисует система, и ему нужен явный фон |
| `global.css` | `color-scheme: dark` | должен следовать за темой, иначе системные элементы останутся тёмными |
| `Base.astro` | `<meta name="theme-color">` и `color-scheme` | цвет полосы браузера на телефоне |
| `--art` | штриховка на пустых местах | задана двумя литеральными цветами внутри градиента |

**Три вещи, которые надо решить до реализации:**

1. **Где хранить выбор.** `localStorage` плюс атрибут на `<html>`, который
   ставится инлайновым скриптом **до первой отрисовки** — иначе тема моргнёт
   при каждой загрузке. Приём в проекте уже применён дважды, для метки
   `data-js` и для [[Хлебные крошки|подстановки в крошках]].
2. **Уважать ли системную настройку.** `prefers-color-scheme` как значение по
   умолчанию, а явный выбор его перебивает — обычная схема, но это три
   состояния, а не два: «светлая», «тёмная», «как в системе».
3. **Цвет установленного приложения не переключится.** `theme_color` и
   `background_color` живут в манифесте, а он статический: у поставленного на
   домашний экран приложения цвет обрамления останется тот, что был при
   установке. Это ограничение формата, обойти нечем.

Про «несколько монохромных»: с точки зрения кода разницы с двумя темами нет —
это те же токены, только наборов больше двух.

</details>

---

## 2. Обновлённый дизайн страницы релиза

Задумка владельца, здесь пока только то, что есть сейчас, — чтобы было от
чего отталкиваться.

**Часть уже сделана 28 августа 2026.** Полоса перестроена: обложка слева,
справа всё остальное — текст прижат к верху, лейбл с жанрами и кнопками к
низу, между ними воздух. Этаж под полосой убран, подложки нет. Разбор — в
[[Страницы]], решения — в [[Решения]].

Что осталось незакрытым:

- на узком экране полоса разворачивается в вертикальный кадр 4:5 с подписью
  под ним — это отдельная раскладка, и она заметно отличается от широкой;
- `More Releases` есть только на 278 страницах из 1102, и на остальных
  страница обрывается блоком фактов;
- страница клипа ту же перестройку не получила: у неё нет даты, и решать её
  раскладку нужно отдельно.

**Вопросы, на которые нужен ответ до работы:** что именно не устраивает —
композиция полосы, набор сведений, или страница целиком? Есть ли новый макет
в Claude Design, или собирать от нынешнего?

---

## 3. Сортировка на Videos

Сейчас два пункта: `By artist` и `By title`. Хочется четыре — оба поля в обе
стороны.

**Делается почти без работы.** Сортировка на странице уже разбирает эти два
поля, добавляется разворот. Единственное, за чем следить, — устойчивость:
порядок с сервера идёт по исполнителю, а внутри по названию, и при развороте
вторичный ключ разворачивается вместе с первым, если просто перевернуть
массив. Правильнее сравнивать явно, а не `reverse()`.

Заодно стоит решить, что делать с подписями: `By artist` превратится в пару,
и `A → Z` / `Z → A` рядом с ними — то же обозначение, что на списках имён.

Даты у клипов нет и не будет, так что «сначала свежие» здесь по-прежнему
невозможно — см. [[Данные]].

---

## 4. Ещё одна сортировка на Artists, Labels, Genres

Сейчас три пункта: `A → Z`, `Z → A`, `Most releases`.

**Предлагаю «по свежему релизу»** — от тех, у кого недавно что-то выходило, к
тем, у кого давно. Данные это выдерживают: дата выхода заполнена у всех 1102
релизов, а значит вычисляется у **973 исполнителей из 990** и у **всех 618
лейблов**. У семнадцати исполнителей без релизов даты нет — им место в конце
списка, как релизам без даты в каталоге.

Это единственная из очевидных добавок, которая показывает что-то новое:
список перестаёт быть справочником и начинает отвечать на «кто из них сейчас
жив».

Прочие кандидаты и почему они слабее:

- **`Fewest releases`** — зеркало к `Most releases`, делается одной строкой,
  но отвечает на вопрос, который вряд ли кто-то задаёт;
- **по стране** — сортировка по коду даёт алфавит стран, а не смысл; страна
  скорее просится в фильтр, чем в порядок;
- **по числу клипов** — клипы есть у полутора десятков исполнителей из 990,
  список выродится.

Для жанров «по свежему релизу» тоже считается и означает то же самое.

---

## 5. ~~Порядок строк в шапке релиза~~ — отклонён 28 августа 2026

Собран и посмотрен вживую; вернулись к прежним заголовкам. Раскладка полосы
из варианта B при этом осталась. Разбор ниже сохранён на случай, если к идее
вернутся, а итог записан в [[Решения]].

Предложение владельца: перестроить верх страницы релиза так, чтобы название
шло первым, а тип и дата читались фразой.

```
сейчас                          предлагается
─────────────────────────       ─────────────────────────
ALBUM · AUGUST 8, 2025          Life in Small Spaces
Life in Small Spaces            Album by Black Marble
Black Marble                    Released August 8, 2025
```

Тип и дата сейчас склеены в одну надстрочную строку над заголовком, а
исполнитель стоит под ним. Предлагается развести: тип уходит к исполнителю,
дата — на свою строку со словом `Released`.

**Данные это выдерживают без оговорок в одном месте и с оговоркой в
другом.**

Дата — без оговорок: она заполнена у всех 1102 релизов и **везде с точностью
до дня**, так что `Released August 8, 2025` читается всегда целиком, а
`formatDate` уже умеет отдавать её в этом виде.

А вот тип — с оговоркой. У двадцати релизов типов два, и девятнадцать из них
— это `Album + Collaboration`; ровно у них же по два исполнителя. То есть в
лоб строка вышла бы такой:

```
Album · Collaboration by Nicholas Payton · Butcher Brown
```

Точка тут работает дважды и в разных смыслах — как перечисление типов и как
перечисление имён, — и фраза перестаёт читаться.

**Что предлагаю решить:**

1. **Оставлять на этой строке только формат** — `Album`, `EP`,
   `Compilation`, — а `Collaboration` не выводить: два имени после `by` и так
   говорят, что это совместная работа. Тег при этом никуда не девается, он
   продолжает работать фильтром на Music.
2. **Чем разделять имена.** Сейчас везде точка. Во фразе `by A and B`
   просится `and`, но это будет единственное место на сайте, где имена
   соединяются словом, — во всех списках и путях стоит точка.

**Чего это коснётся:** `albums/[slug].astro` и три правила в стилях —
`.detail__kicker`, `.detail__title`, `.detail__artist`. Строк там немного,
но раскладка полосы завязана на порядок: сейчас блок прижат к низу
(`justify-content: flex-end`), и третья строка снизу изменит равновесие
полосы 16:7.

**Отдельный вопрос — страница клипа.** У неё та же шапка и тот же порядок, но
даты нет и не будет. Строка вышла бы `Official Video by Yuuf` без третьей —
это работает, но решать надо заодно, иначе две почти одинаковые страницы
разойдутся.

Предложение выглядит цельным, и сделать его недолго. Скажешь — возьмусь,
нужны только ответы на два вопроса выше.

---

## Что стоит держать в уме

Пункты попарно связаны, и порядок работы от этого зависит.

**Две сортировки — 3 и 4** — трогают одно и то же место, списки и их полосу
управления, и стоит делать их одной правкой, а не по очереди: см.
[[Списки и фильтры]].

**Шапка релиза — 2 и 5** — это одна страница. Пятый пункт расписан до
готовности, второй пока лишь заявлен; если делать пятый в одиночку, а потом
взяться за второй, порядок строк может смениться дважды. Разумнее либо
дождаться, что именно не устраивает во втором, либо считать пятый первым
шагом второго и сразу так и записать.

Тема — единственный пункт, ни с чем не пересекающийся: она живёт в
[[Оформление|токенах]].

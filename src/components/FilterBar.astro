---
/* Полоса отбора — отдельной строкой под заголовком, в четыре столбца.

   Столбцы те же, что у решётки обложек: та же ширина, те же вертикальные
   дивайдеры, тот же вылет за поля кадра. На расчётных 1920 черта между
   вторым и третьим столбцом ложится ровно на середину экрана — туда же,
   куда делит полосу заголовка над ней.

   В каждом столбце слева имя отбора с двоеточием, справа сам список.
   Приём из полосы отбора Ghostly: имя оси там стоит подзаголовком над
   своими значениями; здесь значения спрятаны в список, и имя переезжает
   влево от него.

   Полоса липкая и встаёт под заголовок раздела: обе держатся на экране,
   пока листаешь каталог. */
---
<div class="filters-row">
  <slot />
</div>
